; ---------------------------------------------------------------------------------------
; 记录MBR引导过程中的一些操作
; ---------------------------------------------------------------------------------------
read_section:

;清屏操作
clear_screen:
    pusha                  ;把通用寄存器 AX, CX, DX, BX, SP, BP, SI 及 DI 压入栈中
    mov	ax,	0600h
	mov	bx,	0700h
	mov	cx,	0
	mov	dx,	0184fh
	int 10h
	popa                    ;出栈
	ret